# Copy to .env next to the platform compose. Never commit the real .env.
# Generate strong values, e.g.  openssl rand -hex 24

MYSQL_ROOT_PASSWORD=change-me-root
QUERY_DB_PASSWORD=change-me-llm-readonly   # text-to-sql's read-only user
DYR_DB_PASSWORD=change-me-dyr              # DyR demo app user
